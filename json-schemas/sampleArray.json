{
    "type": "object",
    "properties": {
        "sampleIds": {
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "authenticityToken": {
            "type": "string"
        }
    },
    "required": ["sampleIds", "authenticityToken"]
}
